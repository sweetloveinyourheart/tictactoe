import { FunctionComponent } from "react";

interface MathProps { }
 
const Math: FunctionComponent<MathProps> = () => {
    return (
        <section>
            
        </section>
    );
}
 
export default Math;